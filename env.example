# .env.test
NODE_ENV=node_env
DB_URI=uri_complete_de_connexion_a_la_bd   # This is the database URI for the test environment
APP_PORT=port_de_l_application
JWT_SECRET=secret_pour_generer_le_token
DBMS=PostgreSQL # gestionnaire de base donnees (PostgreSQL ou MongoDB)